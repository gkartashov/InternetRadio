<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <import type="android.view.View" />
        <variable
            name="category"
            type="com.jg.internetradio.entity.Category" />
        <variable
            name="callback"
            type="com.jg.internetradio.ui.fragment.categorylist.OnCategoryClick"/>
    </data>
        <android.support.v7.widget.CardView
            android:id="@+id/category_list_card_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:animateLayoutChanges="true"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:layout_marginTop="4dp"
            android:layout_marginBottom="4dp"
            app:onClickListener="@{() -> callback.onClick(category)}"
            android:clickable="true"
            android:focusable="true"
            android:foreground="?android:attr/selectableItemBackground">

            <RelativeLayout
                android:id="@+id/category_list_layout"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_margin="8dp">

                <TextView
                    android:id="@+id/category_title"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="18sp"
                    android:text="@{category.title}" />

                <TextView
                    android:id="@+id/category_description"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_below="@+id/category_title"
                    android:visibility="visible"
                    android:text="@{category.capitalize(category.description)}" />

                <ImageView
                    android:id="@+id/arrow_image"
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:layout_alignParentEnd="true"
                    android:layout_alignTop="@id/category_title"
                    android:src="@drawable/ic_down_arrow"/>

            </RelativeLayout>
        </android.support.v7.widget.CardView>
</layout>